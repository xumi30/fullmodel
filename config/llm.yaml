# AI model config.

defaults:
  profile: qwen

profiles:
  qwen:
    api_key: ${DASHSCOPE_API_KEY}
    provider: qwen
    region: cn-beijing

brains:
  text:
    model: qwen-plus

  vision:
    model: qwen-vl-plus

  voice:
    model: cosyvoice-v3-flash

  image:
    model: qwen-image-2.0-pro
